/** Matches `browseAuctionsRoute` validateSearch — use for `Link` / `navigate` defaults. */
export const DEFAULT_BROWSE_SEARCH = {
  q: undefined as string | undefined,
  category: undefined as string | undefined,
  status: undefined as string | undefined,
  end_after: undefined as string | undefined,
  end_before: undefined as string | undefined,
  ordering: undefined as string | undefined,
  page: 1,
}
